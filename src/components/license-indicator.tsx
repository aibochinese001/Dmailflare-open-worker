// All features are unlocked for free on this deployment, so the upgrade
// indicator is intentionally disabled. The component is kept as a no-op
// because the admin/dashboard/settings layouts still render it.
export function LicenseIndicator() {
	return null;
}
